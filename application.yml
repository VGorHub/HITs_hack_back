spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_reminder
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
yaml server:
  address: 0.0.0.0
  port: 8080

token:
  signing:
    key: 53A73E5F1C4E0A2D3B5F2D784E6A1B423D6F247D1F6E5C3A596D635A75327855

telegram:
  bot:
    username: ai_reminder_bot      # @username бота
    token:    ${TG_BOT_TOKEN}      # храните секрет в ENV-переменной
websocket:
  endpoint: /ws                   # URL для подключения SockJS
  prefix:
    app:   /app                   # куда клиент посылает
    topic: /topic                 # откуда клиент слушает
